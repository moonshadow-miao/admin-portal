<template>
  <div class="list">
    <el-table header-row-class-name="header" size="mini" :data="data" border style="width: 100%">
      <el-table-column fixed v-if="selection" type="selection" width="55"></el-table-column>
      <slot></slot>
    </el-table>
    <div class="page clearfix">
      <el-pagination class="fr pagination" background @size-change="handleSizeChange" @current-change="handleCurrentChange" :current-page="currentPage" :page-sizes="[5,10,20,50,100,200]" :page-size="10" layout="total, sizes, prev, pager, next, jumper" :total="400">
      </el-pagination>
    </div>
  </div>
</template>

<script>
  export default {
    name: "list",
    data() {
      return {
        currentPage: 1
      }
    },
    props: {
      selection: {
        type: Boolean,
        default: false
      },
      data: {
        type: [Object, Array],
        required: true
      }
    },
    methods: {
      handleSizeChange(val) {
        console.log(`每页 ${val} 条`);
      },
      handleCurrentChange(val) {
        console.log(`当前页: ${val}`);
      }
    }
  }

</script>

<style rel="stylesheet/scss" lang="scss">
  .header {
    th {
      padding: 2px;
      line-height: 14px;
      background-color: #f7f7f7;
    }
  }

  .list {
    padding: 0 10px;
  }

  .page {
    padding: 15px 20px;
    .pagination {

    }
  }
</style>
